// Admin Setup Page
// Creates the first admin user for the application

'use client';

import { AdminSetup } from '@/components/auth';

export default function SetupPage() {
  return <AdminSetup />;
}
